---
"@shazhou/proman": patch
---

### fix

- Skip private packages during publish (#61, #62)
  - Check both proman.yaml `private: true` and package.json `"private": true`
  - Log `⏭ skipped @org/pkg (private)` instead of failing with EPRIVATE

- Skip already-published packages instead of aborting (#66, #67)
  - When pnpm returns "cannot publish over the previously published versions", log `⏭ skipped @org/pkg@version (already published)` and continue
  - Real errors (auth, network) still abort as before

### refactor

- Simplify publish loop, eliminate indexOf reference dependency (#64)

### chore

- Add pre-push hook for lint + test (#63)
