{
  "name": "@shazhou/proman",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "proman": "./dist/cli.js"
  },
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "build": "bun build ./src/cli.ts --target=bun --outdir=dist --external @biomejs/biome --external typescript --external yaml"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@biomejs/biome": "^2.4.16",
    "typescript": "^5.5.0",
    "vite": "^5.0.0",
    "wrangler": "^3.0.0",
    "yaml": "^2.9.0"
  }
}
