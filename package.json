{
  "name": "@shazhou/proman",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "proman": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "build": "bun build ./src/cli.ts --target=bun --outdir=dist"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
