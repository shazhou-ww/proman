{
  "name": "@shazhou/proman",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "proman": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "build": "bun build ./src/cli.ts --target=bun --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@biomejs/biome": "^2.4.16",
    "typescript": "^5.5.0",
    "yaml": "^2.9.0"
  }
}
